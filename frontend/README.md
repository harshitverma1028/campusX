# Frontend - Campus Marketplace (Vite + React)

## Setup

1. Install dependencies: `npm install`
2. Start dev server: `npm run dev` (default on http://localhost:5173)
3. Make sure backend is running on http://localhost:5000
